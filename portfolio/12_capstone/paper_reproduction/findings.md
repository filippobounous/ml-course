# PPO clip-ε ablation — findings

> *Auto-generated by `ppo_clip_ablation.py` on first run. The file you're
> looking at is the placeholder.*

Run the reproduction to populate this file:

```bash
make -C portfolio/12_capstone/paper_reproduction reproduce
# or, for a CI smoke run (~30 s):
python portfolio/12_capstone/paper_reproduction/ppo_clip_ablation.py --quick
```

Expected sections after a real run:

- **Ablation table** — final mean reward + std for $\varepsilon \in \{0.1, 0.2, 0.3, \text{no-clip}\}$.
- **What I saw that surprised me** — short reflection (a few sentences).
- Companion figure: `figure_clip_ablation.png`.
